Check what task you currently have claimed.

```bash
./.opus-orchestra/task-claimer.sh my-task "$(git branch --show-current)"
```

If you have a task claimed, read it at `.opus-orchestra/backlog/<task-name>.md`.
