{
  "name": "@opus-orchestra/core",
  "version": "0.3.0",
  "description": "Core logic for Opus Orchestra - platform-agnostic agent management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext ts",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist",
    "coordination"
  ],
  "dependencies": {
    "async-mutex": "^0.5.0",
    "chokidar": "^5.0.0",
    "p-retry": "^7.1.1",
    "pino": "^10.1.1",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "@vitest/coverage-v8": "^4.0.0",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.x",
    "vitest": "^4.0.0"
  }
}
